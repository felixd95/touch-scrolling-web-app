# The SageMaker PyTorch container already ships torch, so only GPyTorch
# needs to be installed. The SageMaker inference toolkit pip-installs this
# file into the container at model load time.
#
# inference.py implements its own minimal ICM ("Hadamard") multi-task GP
# directly on top of GPyTorch (see _MultiTaskGPModel) instead of using
# botorch.models.MultiTaskGP. This drops botorch and its own extra
# transitive dependencies (pyro-ppl, multipledispatch, ninja, etc.) from
# the cold-start dependency-install path entirely, while keeping the same
# ICM model architecture.
#
# gpytorch>=1.15 requires torch>=2.0, satisfied by the torch version shipped
# in the pytorch-inference:2.6.0 DLC.
gpytorch==1.15.2

